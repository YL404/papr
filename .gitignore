# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.pnpm-store/
dist
dist-ssr
*.local

# Rust / Cargo build output (the Cargo workspace root target dir)
/target

# Stray compiler scratch output
a.out

# pnpm allowBuilds local config (generated by `pnpm approve-builds`)
pnpm-workspace.yaml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# ralph-loop local state
.claude/

# Local agent workflow notes — not part of the repo
AGENTS.md
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
